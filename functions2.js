/* 
//scope
let one = 1;
console.log(one);

function diffOne() {
  let one = 2;
  console.log(one);
}
diffOne();
console.log(one);
*/

 //Quiz 1

 /* //          ***STUDY THIS ONE***

 function makeLine(length) {
  let line = "";
  for (let j = 1; j <= length; j++) {
    line += "* ";
  }
  return line + "\n";
}

function buildTriangle(x) {
  let tri = "";
  for (let i = 1; i <= x; i++) {
    tri += makeLine(i);
  }
  return tri;
}

console.log(buildTriangle(10));
*/

